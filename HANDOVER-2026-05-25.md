# HANDOVER — 뮤직매치 (Music Dating App)

> 다음 작업자(사람/AI)에게 넘기는 **현재 기준** 인수인계 문서.
> 마지막 갱신: **2026-05-25** · main = `507218b` (PR #67까지 머지)
> 기존 `HANDOVER.md`(5/21)·`PROJECT-STATUS.md`(5/22)·`ONBOARDING.md`(5/24)는 일부 stale — **이 문서가 최신 기준**.

---

## 0. 30초 요약
대학생 대상 **음악 취향 기반 1:1 매칭 + 실시간 채팅** 모바일 웹앱(PWA). 멋사 1조 축제용.
Remix v2 + Supabase + Vercel. 핵심 플로우(가입→프로필→승인→곡선택→매칭→채팅→알림)는 **전부 동작**.
현재는 기능 보강·UX 다듬기 단계이며, **열린 draft PR 다수 + 머지 전 적용 필요한 SQL 마이그레이션 몇 건**이 핵심 미해결 사항.

## 1. 좌표 (현재값)
| 항목 | 값 |
|------|-----|
| Repo | `iluv4/music-dating-app-keonuu` (main) |
| 개발 브랜치 규칙 | **PR마다 새 브랜치** (같은 브랜치 연속 스쿼시 머지 금지 — main과 갈라짐) |
| 배포 | Vercel `music-dating-app-keonuu.vercel.app` (main 자동배포) |
| Supabase 프로젝트 | `exobcejmlbqylutmxqtj` |
| 디자인 | Figma `707rxeVk0SGe1nZB4BE0LR` |
| 브랜드 메인색 | **#ff625d (코랄)**, soft `#ffeeed` — 유일 기준. 마젠타 `#ff0558` 폐기(husky `lint:colors`가 차단) |

## 2. 실행 / 검증
```bash
npm install
npm run dev          # http://localhost:3000
npm run type-check   # tsc
npm run build        # remix build
npm run lint:colors  # 폐기색 가드 (husky pre-commit에서도 자동 실행)
```
- 로컬 `.env`가 없으면 인증/DB 화면이 500 → **시각 검증은 prod 머지 후**. Vercel 프리뷰도 Supabase env가 Production 스코프뿐이라 인증/DB 화면이 죽음.
- 코드 변경 후 **type-check + build 통과**는 필수.

## 3. 기술 스택
| 영역 | 사용 |
|------|------|
| 프레임워크 | Remix v2.17 (Node, SSR) + React 18 + TS |
| DB/인증/실시간 | Supabase (Postgres + Auth + Realtime + RLS) |
| 배포 | Vercel (`@vercel/functions`) |
| 외부 연동 | 멜론 차트/검색 (`melona` 스크래핑, 실패 시 `song-fallback.json` 30곡) |
| 푸시 | Web Push (`web-push`, VAPID) |
| 분석 | PostHog (`posthog-js`) — 키 주입 시 작동 |
| UI | 폰 프레임 모바일 웹, 인라인 style + 디자인 토큰(`constants.ts`), Pretendard |

## 4. 코드 구조 (핵심)
```
src/
├ lib/
│  ├ db-types.ts            ★ DB row 타입 단일 소스
│  ├ auth.server.ts         ★ requireUser / requireApprovedUser / requireMatchAccess
│  ├ supabase.server.ts / .client.ts / supabase-admin.server.ts (service_role)
│  ├ constants.ts           ★ COLORS(#ff625d)/TYPOGRAPHY 토큰
│  ├ melon-chart.server.ts / melona-cache.server.ts  멜론 차트·검색 캐시
│  ├ push.server.ts / push.client.ts                 Web Push
│  ├ analytics.client.ts    PostHog
│  ├ slack.server.ts        운영 알림(Slack)
│  ├ clubs.ts / departments.ts / terms-content.ts / icebreakers.ts  정적 데이터
│  ├ preview-data.ts        비로그인 둘러보기 더미 데이터
│  └ repos/                 ★ DB 접근 계층 (라우트에서 직접 supabase.from() 금지)
│     ├ profiles / user-songs / matches / messages / notifications .server.ts
├ components/  PhoneFrame, BottomNav, Button, TextInput, Song* 등
└ routes/
   ├ 가입: welcome / terms / signup / login / logout / auth.callback
   ├ 프로필: profile(.basic/.school/.payment/.edit/._index)
   ├ 승인대기: waiting / explore(디버그, prod 숨김)
   ├ 곡: genre / music-select / music / api.melon.{search,chart}
   ├ 매칭/채팅: chat._index / chat.$matchId / chat.preview / rematch
   ├ 알림: notifications / api.push.subscribe
   ├ 멤버: member.$userId
   └ 관리: admin (ADMIN_KEY 미설정 시 404 가드)
```

## 5. 데이터 모델 (현재 db-types 기준)
- **profiles** — name, birth_year, gender, school, major, **club**, bank_holder, **is_approved**(승인 게이트)
- **user_songs** — 선택 곡 1~3개 (매칭 시 곡 일치 비교)
- **matches** — user_a<user_b, status: **pending|active|ended** (pending = 재매칭 운영팀 확인 대기)
- **messages** — match별. `image_url, read_at, edited_at, deleted_at, view_once, viewed_at, disappear`(펑/한 번만 보기/수정·삭제 지원)
- **notifications** — type: match|message|system
- 전 테이블 RLS 활성. 매칭/채팅종료/탐색/멤버상세/알림은 SECURITY DEFINER RPC·트리거로 처리.

## 6. 그동안 한 일 (5/21 이후 main 머지분, PR# 기준)
- **#67** 재매칭 동시성 수정 + k6 동시 매칭 검증 하네스(`loadtest/`)
- **#55** 동시 매칭 경합 방지 (advisory lock + active 쌍 유니크 인덱스)
- **#59** 채팅 메시지 수정/삭제 · 펑(disappear) · 한 번만 보기 사진 + 캡처 억제
- **#58** 구글 로그인 제거 + 로그인 없이 전체 미리보기(둘러보기)
- **#57** 재매칭 "한 명 더" — 즉시 연결 대신 운영팀 확인(pending) 방식
- **#54/#22/#23** 채팅·DB 성능/보안 강화 (advisor 대응)
- **#48** 토스식 인터랙션 (화면 전환·바텀시트·탭 피드백)
- **#40/#45/#46** PWA Web Push 알림 + iOS 권한/홈화면 추가 안내
- **#27/#29** 채팅 읽음 표시 + 사진 로드 후 스크롤 유지
- **#20/#36** 가입 IP rate limit + 가입 퍼널 뒤로가기 루프 수정
- 그 외 다수 UI 정합/디자인 토큰 통일 (#12~#44, #60/#61 등)

## 7. ⚠️ 지금 열린 작업 (인수인계 핵심)

### 7.1 열린 draft PR (머지 검토 대기)
| PR | 내용 | 머지 전 필요 |
|----|------|------|
| **#66** | 매칭 조건 완화 (곡 일치 → 장르 겹침 OR 곡 겹침) | **SQL** `migration_genre_matching.sql` |
| **#65** | 체험 AI 모의 대화(`/chat/ai`) + 프로필 사진(입구컷·매칭 후 공개) | **SQL** `migration_profile_photo.sql`, (선택) `ANTHROPIC_API_KEY` |
| **#64** | ONBOARDING 현행화 (문서) | — |
| **#63** | 멜론 검색 결과 캐싱 + 장애 시 폴백 필터 | — |
| **#62** | 프로필 사진 업로드 (탐색/멤버/마이 노출) | **SQL** `migration_profile_photo.sql` |
| **#61** | 출생연도 placeholder 안내 문구화 | — |
| **#60** | welcome/signup 로그인 유도 문구 다듬기 | — |
| **#56** | 첫 메시지 추천 문구 MZ 카톡체로 | — |
| 오래됨 #11/#14/#18/#19 | Vercel Analytics / 마스코트 / a11y / 컬러정리 — 검토 후 정리(머지 or close) |

> #62와 #65는 둘 다 프로필 사진을 다룸 → **충돌 가능, 하나로 정리 권장**.

### 7.2 머지 전 적용 필요한 SQL (코드 머지와 별개로 SQL Editor/`db:apply` 실행)
- `migration_genre_matching.sql` (PR #66) — `profiles.genres text[]` + GIN 인덱스
- `migration_profile_photo.sql` (PR #62/#65) — `profiles.photo_path` + 비공개 버킷 + RLS + RPC
- 미적용 시 해당 기능에서 프로필 저장/사진이 깨짐.

## 8. DB 마이그레이션 적용 방법
SQL은 `supabase/*.sql`. 적용 2경로:
1. **권장 — 스크립트:** `npm run db:apply -- supabase/<파일>.sql` (`.env`에 `SUPABASE_DB_URL` 필요; Dashboard→Project Settings→Database→Connection string→URI)
2. **대시보드:** SQL Editor 붙여넣기 → Run (Supabase MCP 간헐 끊김 시 폴백)

기존 적용된 마이그레이션 목록은 `supabase/` 디렉터리 참고 (match_concurrency, chat_advanced/photos, club, push 관련 등 다수).

## 9. 관리자 운영
- **가입 승인**: Supabase Table Editor → profiles → `is_approved` = true (트리거가 `approved_at` 자동). 또는 `/admin`(ADMIN_KEY 필요).
- **매칭**: 앱 내 매칭 RPC(`find_or_create_match`)로 사용자 주도 + 재매칭은 pending → 운영팀 확인.
- **재매칭**: 사용자 재결제 → pending matches 확인 후 active 전환.

## 10. 알려진 함정
- **Vercel env는 추가만으론 런타임 미반영 → 재배포 필수.** Production 스코프 체크.
- 멜론 라이브 검색이 샌드박스/특정 환경에서 403 → 폴백 동작(#63에서 보강).
- 가입 흐름은 sessionStorage 의존 → **단일 탭에서만** (새 탭 진입 시 폼 유실).
- Supabase MCP 간헐 끊김(`net::ERR_FAILED`) → `db:apply` 또는 SQL Editor로 우회.
- 프로필 사진 교체 시 옛 객체 미정리(고아 객체) — 후속 정리 필요.

## 11. 다음 작업 제안
- **열린 PR 정리** — #60/#61/#56/#63 등 무위험 다듬기 머지, #62↔#65 사진 중복 정리, 오래된 #11/#14/#18/#19 처리.
- **사진/장르매칭 SQL 적용 후 prod 검증** (#62·#65·#66).
- **푸시 활성화** — Vercel env `VAPID_PUBLIC_KEY/PRIVATE_KEY/SUBJECT` + 재배포 (iOS는 PWA 홈화면 추가 필요).
- (선택) 체험 AI 실 LLM — Vercel env `ANTHROPIC_API_KEY`.
- 결제 자동화·신고/차단은 이번 버전 스코프 아웃(무통장입금 수동 유지).

## 12. 참고 문서
| 파일 | 내용 |
|------|------|
| `ONBOARDING.md` | 5분 온보딩 (좌표·실행·DB·하네스) |
| `PROJECT-STATUS.md` | 5/22 현황 스냅샷 |
| `HANDOVER.md` | 초기 상세 인수인계 (스키마/RLS/트리거 원문 — 일부 stale) |
| `research.md` | Figma 분석 + 디자인 토큰 + 백엔드 아키텍처 |
| `plan_kim_0521_*.md` | 단계별 구현 계획 (의사결정 컨텍스트) |
| `.telemetry/` | 트래킹 플랜 |
| `CLAUDE.md` | 협업 방식·워크플로·코드 규칙 (항상 로드) |
</content>
</invoke>

-- ============================================================
-- 연락처(phone) 컬럼 추가 — 회원가입 기본정보 단계에서 수집 (idempotent)
--
-- 운영팀이 매칭 성사 후 연락(입금 확인·안내 등)할 수 있도록 휴대폰 번호를 저장.
-- 저장 형식: 숫자만(하이픈 제거), 예) 01012345678
-- 타 사용자에게는 노출하지 않는다(RLS 그대로 — 본인 행만 조회 가능, admin 은 service_role).
-- ============================================================

alter table public.profiles add column if not exists phone text;
